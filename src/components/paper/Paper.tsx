import styled from "@emotion/styled";

export const Paper = styled("div")(({ theme }) => {
  return {
    backgroundColor: "#333",
    borderRadius: "0.25rem",
    boxShadow: "0 1px 0.5rem #0008",
    overflow: "hidden",

  }
});